specVersion: "v1beta"
serviceId: "reinom"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "reinom-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
